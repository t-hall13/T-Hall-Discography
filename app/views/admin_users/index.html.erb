<% @page_title = "Admin Users List" %>

<%= link_to("<<Back To Menu", {:controller => 'access', :action => 'index'}, :class => 'back_link') %>

<div class="user index">

	<%= link_to(image_tag('plus_sign.png', :size => '11 x 11', :alt => '+') +" Add New Admin User", {:action => 'new'}, :class => 'action new') %>

		<div><%= pluralize(@admin_users.size, 'admin_user') %> found</div>

		<table class="listing" summary = "Admin Users list">
			<tr class= "header">
				<!--<th>&nbsp;</th>-->
				<th>Username</th>
				<th>Name</th>
				<th>Email</th>
				<th>Actions</th>

			</tr>
			<% @admin_users.each do |admin_user| %>
			<tr class="<%= cycle('odd', 'even') %>">
				<td><%= admin_user.username %></td>
				<td><%= admin_user.name %></td>
				<td><%= mail_to(admin_user.email) %></td>

				<td class="actions">
					
					<%= link_to("Edit", {:action => 'edit', :id => admin_user.id}, :class => 'action edit') %>
					<%= link_to("Delete", {:action => 'delete', :id => admin_user.id}, :class => 'action delete') %>
				</td>
			</tr>
			<% end %>
		</table>
		</div>
</div>